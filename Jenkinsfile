pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building the application...'
                // your build commands
            }
        }

        stage('Test') {
            steps {
                echo 'Running tests...'
                // your test commands
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying the application...'
                // your deploy commands
            }
        }
    }
}

